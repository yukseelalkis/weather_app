import 'package:flutter/material.dart';

mixin ProjectItemColors {
  // Ana arka plan rengi - Gradient ile daha canlı bir görünüm
  static const Color mainColorStart = Color(0xFF4A90E2); // Gradient başlangıcı
  static const Color mainColorEnd = Color(0xFF50E3C2); // Gradient bitişi

  // AppBar ön plan rengi
  static const Color appBarForeColor = Colors.white;

  // Scaffold arka plan rengi
  static const Color scafoldBackRoundColor =
      Color(0xFFF5F7FA); // Hafif açık gri

  // Kartların arka plan rengi - Minimalist beyaz
  static const Color cardColor = Color(0xFFFFFFFF); // Beyaz

  // Gölge rengi - Hafif gölge
  static const Color shadowColor = Color(0xFFB0BEC5); // Yumuşak gri

  // Yazı için koyu gri renk
  static const Color textColor = Color(0xFF263238); // Hafif koyu gri

  // Vurgu rengi - Canlı turuncu
  static const Color accentColor = Color(0xFFFF6F61); // Turuncu

  // İkincil vurgu rengi - Hafif sarı
  static const Color secondaryAccent = Color(0xFFFFD54F); // Sarı

  // Dropdown yazı rengi
  static const Color dropDownTextColor = Colors.white;
  //App bar Bacround Color
  static const Color appBarBackRound = Color(0xFF37474F);
}
